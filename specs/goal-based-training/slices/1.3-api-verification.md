# Slice 1.3: Wire contract and end-to-end verification

## Objective

Finish the client-facing contract and prove the complete profile → starter copy →
dated schedule → set checks → status → participation flow. This slice is complete
only when `./prds/goal-based-training/run-prd-test.sh` exits 0.

## Signal

**Signal Skill:** None

**Expected Behavior:**
- `./prds/goal-based-training/run-prd-test.sh` exits 0.
- `go test ./...` passes.
- `scripts/smoke.sh` contains and can exercise the authenticated happy path.
- `scripts/smoke-toggle.sh` remains the release-time runtime check.

## Required outcomes

- Verify the slice 1.1 `docs/CONTRACTS.md` route/shape contract remains exact;
  generated Swagger agrees with it.
- Swagger annotations/generated `docs/` agree with the contract.
- Smoke flow uses stable fixture IDs/operation keys and verifies `primary_goal`,
  a dated scheduled workout, required-set completion, an incomplete outcome, an
  extra/off-plan set, and `participation` separately.
- Add acceptance-focused Go tests named around Goal/Program/Schedule/Participation
  so regressions are discoverable.
- Tests cover populated migration-011 upgrade, six-goal starter coverage,
  planned/in-progress/finalized lifecycle, multiple session IDs on one date,
  lazy timezone finalization, 409 revision conflicts, immutable exercise/history,
  all-history delete rejection, legacy adoption, and account deletion cleanup.
- Preserve the pre-existing API suite and >90% service/middleware coverage goal.
